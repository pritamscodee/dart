| Method / Property     | Work                                 |
| --------------------- | ------------------------------------ |
| `add()`               | Add one item to end                  |
| `addAll()`            | Add multiple items                   |
| `insert()`            | Insert item at specific index        |
| `insertAll()`         | Insert multiple items at index       |
| `remove()`            | Remove first matching value          |
| `removeAt()`          | Remove item by index                 |
| `removeLast()`        | Remove last item                     |
| `removeWhere()`       | Remove items matching condition      |
| `retainWhere()`       | Keep only matching items             |
| `clear()`             | Remove all items                     |
| `contains()`          | Check if value exists                |
| `indexOf()`           | Find first index of value            |
| `lastIndexOf()`       | Find last index of value             |
| `sort()`              | Sort list                            |
| `shuffle()`           | Randomize order                      |
| `sublist()`           | Get portion of list                  |
| `join()`              | Convert list to string               |
| `map()`               | Transform each item                  |
| `where()`             | Filter items                         |
| `whereType()`         | Filter by type                       |
| `forEach()`           | Loop through items                   |
| `any()`               | Check if any item matches            |
| `every()`             | Check if all items match             |
| `take()`              | Take first N items                   |
| `skip()`              | Skip first N items                   |
| `reduce()`            | Combine items into one value         |
| `fold()`              | Reduce with initial value            |
| `expand()`            | Flatten nested lists                 |
| `toList()`            | Convert iterable to list             |
| `toSet()`             | Convert list to set                  |
| `asMap()`             | Convert list to indexed map          |
| `reversed`            | Reverse order                        |
| `followedBy()`        | Append iterable                      |
| `fillRange()`         | Fill range with value                |
| `replaceRange()`      | Replace range of items               |
| `setAll()`            | Replace values from index            |
| `setRange()`          | Copy range into list                 |
| `getRange()`          | Get range as iterable                |
| `removeRange()`       | Remove range of items                |
| `replaceFirst()`      | Replace first match (strings mostly) |
| `first`               | First item                           |
| `last`                | Last item                            |
| `single`              | Only item in list                    |
| `length`              | Total items count                    |
| `isEmpty`             | Check empty                          |
| `isNotEmpty`          | Check not empty                      |
| `firstWhere()`        | First item matching condition        |
| `lastWhere()`         | Last item matching condition         |
| `singleWhere()`       | Single matching item                 |
| `elementAt()`         | Get item at index                    |
| `cast()`              | Convert type                         |
| `takeWhile()`         | Take until condition fails           |
| `skipWhile()`         | Skip until condition fails           |
| `indexWhere()`        | Find index using condition           |
| `lastIndexWhere()`    | Find last matching index             |
| `removeWhere()`       | Delete matching items                |
| `retainWhere()`       | Keep matching items only             |
| `List.generate()`     | Create dynamic list                  |
| `List.filled()`       | Create fixed value list              |
| `List.empty()`        | Create empty list                    |
| `List.from()`         | Create copied list                   |
| `List.of()`           | Create typed copied list             |
| `List.unmodifiable()` | Create immutable list                |
| `[]`                  | Access item by index                 |
| `[]=`                 | Update item by index                 |
| `...`                 | Spread operator merge                |
| `...?`                | Null-aware spread                    |
| `collection if`       | Conditional items in list            |
| `collection for`      | Loop-generated items in list         |
